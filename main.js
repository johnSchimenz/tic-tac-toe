"use strict";

// Gameboard object
const gameBoard = (() => {
    const cellArray = ["", "", "", "", "", "", "", "", ""];
    return cellArray;
})();

// Players objects
const Player = (name, marker) => {
    return {name, marker};
}

// Game-flow object
const gameFlow = (() => {
    
})();